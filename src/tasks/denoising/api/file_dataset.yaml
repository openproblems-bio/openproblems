type: file
example: "resources_test/common/pancreas/dataset.h5ad"
info:
  label: "Preprocessed dataset"
  summary: A dataset containing raw counts and a dataset id.
  slots:
    layers: 
      - type: integer
        name: counts
        description: Raw counts
        required: true
    uns:
      - type: string
        name: dataset_id
        description: "A unique identifier for the dataset"
        required: true
