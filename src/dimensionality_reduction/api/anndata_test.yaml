type: file
description: "The test data"
example: "test.h5ad"
info:
  short_description: "Test data"
  slots:
    layers: 
      - type: integer
        name: counts
        description: Raw counts
        required: true
    var:
      - type: float
        name: hvg_score
        description: high variability gene score. The greater, the more variable.
        required: true
    uns:
      - type: string
        name: dataset_id
        description: "A unique identifier for the dataset"
        required: true
