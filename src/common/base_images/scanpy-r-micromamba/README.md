# Scanpy image with R in micromamba

Build and tag:

```shell
docker build -t mumichae/scanpy-r-micromamba:1.9.1 .
```

Run the image interactively

```shell
docker run -it mumichae/scanpy-r-micromamba:1.9.1 bash
```