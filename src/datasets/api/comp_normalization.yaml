functionality:
  namespace: "datasets/normalization"
  info:
    type_info:
      label: Dataset normalization
      description: |
        A normalization method which processes the raw counts output by a dataset loader.
  arguments:
    - name: "--input"
      __merge__: anndata_raw.yaml
      required: true
    - name: "--output"
      direction: output
      __merge__: anndata_normalized.yaml
      required: true
    - name: "--layer_output"
      type: string
      default: "normalized"
      description: The name of the layer in which to store the normalized data.
    - name: "--obs_size_factors"
      type: string
      default: "size_factors"
      description: In which .obs slot to store the size factors (if any).
  test_resources:
    - path: /resources_test/common/pancreas
      dest: resources_test/common/pancreas
    - type: python_script
      path: /src/common/comp_tests/run_and_check_adata.py
