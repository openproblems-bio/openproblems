__merge__: file_normalized.yaml
type: file
example: "resources_test/common/scrnaseq/pancreas/svd.h5ad"
label: "Dataset+SVD"
summary: "A normalised dataset with a SVD embedding"
info:
  format:
    type: h5ad
    obsm:
      - type: double
        name: X_svd
        description: The resulting SVD embedding.
        required: true