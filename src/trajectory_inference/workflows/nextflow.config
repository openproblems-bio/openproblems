manifest {
  nextflowVersion = '!>=20.10.0'
}

includeConfig "$launchDir/target/nextflow/trajectory_inference/datasets/download_datasets/nextflow.config"

docker {
  runOptions = "-v $launchDir:$launchDir"
}

process {
  maxForks = 30
  container = 'ubuntu'
  errorStrategy='ignore'
}
